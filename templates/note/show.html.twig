{% extends 'base.html.twig' %}

{% block title %}Note{% endblock %}

{% block body %}
    <h1>Note</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ note.value }}</td>
            </tr>
            <tr>
                <th>Field</th>
                <td>{{ note.field }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('note_crud_index') }}">back to list</a>

    <a href="{{ path('note_crud_edit', {'id': note.id}) }}">edit</a>

    {{ include('note_crud/_delete_form.html.twig') }}
{% endblock %}
